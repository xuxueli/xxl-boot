<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.boot.api.framework.mapper.DbTableMapper">

    <resultMap id="DbTable" type="com.xxl.boot.api.framework.model.entity.DbTable">
        <result column="table_name" property="tableName" />
        <result column="table_comment" property="tableComment" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <resultMap id="DbTableColumn" type="com.xxl.boot.api.framework.model.entity.DbTableColumn">
        <result column="column_name" property="columnName" />
        <result column="column_comment" property="columnComment" />
        <result column="column_type" property="columnType" />
        <result column="is_nullable" property="isNullable" />
        <result column="column_key" property="columnKey" />
        <result column="extra" property="extra" />
        <result column="ordinal_position" property="ordinalPosition" />
    </resultMap>

    <select id="listDbTable" parameterType="java.util.Map" resultMap="DbTable">
        SELECT TABLE_NAME AS table_name, TABLE_COMMENT AS table_comment, CREATE_TIME AS create_time
        FROM information_schema.TABLES
        WHERE TABLE_SCHEMA = (SELECT DATABASE())
        <if test="tableName != null and tableName != ''">AND TABLE_NAME LIKE CONCAT('%', #{tableName}, '%')</if>
        ORDER BY CREATE_TIME DESC LIMIT #{offset}, #{pagesize}
    </select>

    <select id="listDbTableCount" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(1) FROM information_schema.TABLES
        WHERE TABLE_SCHEMA = (SELECT DATABASE())
        <if test="tableName != null and tableName != ''">AND TABLE_NAME LIKE CONCAT('%', #{tableName}, '%')</if>
    </select>

    <select id="listDbTableColumn" parameterType="java.util.Map" resultMap="DbTableColumn">
        SELECT COLUMN_NAME AS column_name, COLUMN_COMMENT AS column_comment,
               COLUMN_TYPE AS column_type, IS_NULLABLE AS is_nullable,
               COLUMN_KEY AS column_key, EXTRA AS extra, ORDINAL_POSITION AS ordinal_position
        FROM information_schema.COLUMNS
        WHERE TABLE_SCHEMA = (SELECT DATABASE()) AND TABLE_NAME = #{tableName}
        ORDER BY ORDINAL_POSITION
    </select>

</mapper>
